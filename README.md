# DreamForge AI (MVP)

🚀 DreamForge AI turns your imagination (sketch or voice) into a working web app.

## How to Run
1. Clone repo  
2. Install backend:  
   ```bash
   cd backend
   pip install -r requirements.txt
   uvicorn app.main:app --reload

## 🧠 Groq AI Setup

This project now uses **Groq API** instead of OpenAI for AI orchestration.

1. Get your API key from [https://console.groq.com/](https://console.groq.com/)
2. Create `.env` file in root:
